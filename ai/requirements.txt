# AI service deps (ASCII-only comments so pip on Windows GBK locale can parse this file)
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart>=0.0.17

# LangChain / LLM (0.3.0 has no py3.13 wheels on PyPI; use >=0.3.9 for Python 3.13)
langchain>=0.3.9,<0.4
langchain-openai>=0.2.0,<0.3
openai>=1.51.0,<2

# Document parsing
pypdf==5.0.0
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5

# Utilities
pydantic>=2.9.0,<3
python-dotenv==1.0.1
httpx==0.27.0

# Optional: pgvector / sqlalchemy pins reserved for future use
# pgvector==0.2.5
# sqlalchemy==2.0.35
